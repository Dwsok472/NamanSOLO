import styled from 'styled-components';

const PageWrapper = styled.div`
  margin-top: 100px;
  padding: 20px;
`;

const Home = () => {
  return (
    <PageWrapper>

    </PageWrapper>
  );
};

export default Home;
